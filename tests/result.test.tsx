import { render,screen } from '@testing-library/react'; import { MemoryRouter,Route,Routes } from 'react-router-dom'; import { describe,expect,it } from 'vitest'; import { ResultPage } from '../src/pages/ResultPage';
describe('halaman hasil',()=>{it('menampilkan peserta dan meja',()=>{render(<MemoryRouter initialEntries={[{pathname:'/hasil/x',state:{participant:{id:'x',participant_id:'001',name:'Sari Wangi',institution:'Bappeda',region:'Sleman',province:'DIY',table_number:7}}}]}><Routes><Route path="/hasil/:participantId" element={<ResultPage/>}/></Routes></MemoryRouter>);expect(screen.getByText('MEJA 7')).toBeInTheDocument();expect(screen.getAllByText(/Sari Wangi/).length).toBeGreaterThan(0)})});
